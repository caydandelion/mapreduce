TextAnalyse文件夹下是文件预处理的代码，包括选取特征词与文本向量化。

InputText里是本次实验的初始数据集。

OutputText里有特征词（FeatureWords）,训练集向量（TrainVector）,测试集向量(TestVextor),
knn的结果（knn）,Bayes的结果（Bayes）。

knn里是knn的代码。

NaiveBayes里是NaiveBayes的代码。
